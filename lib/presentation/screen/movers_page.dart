import 'package:flutter/material.dart';

class MoversPage extends StatelessWidget {
  const MoversPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('No Movers Found'),),);
  }
}